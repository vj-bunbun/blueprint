{
  "name": "blueprint",
  "version": "0.1.0",
  "description": "Provider-agnostic system prompt assembly. Own your prompts.",
  "license": "MIT",
  "repository": "https://github.com/vj-bunbun/blueprint",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "init": "bun run init.ts",
    "inspect": "bun run inspect.ts"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
