---
section: 
order: 
budget:            # max tokens (optional)
cache: static      # static | dynamic
required: false    # true = never cut when budget is tight
---

# Section Title

(Section content — assembled into the system prompt in order)
